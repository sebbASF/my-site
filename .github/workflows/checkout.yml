name: Checkout

on:
  workflow_dispatch:
  push:
    paths:
      - '**/checkout.yml'

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Main
        uses: actions/checkout@v4
      - name: List 1
        run: |
          ls -l
          touch output.tmp
      - name: Checkout Destination
        run: |
          ls -l
          git checkout gha-site
      - name: List 2
        run: |
          ls -l
